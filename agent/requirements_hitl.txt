# Requirements for CandleZip HITL Agent
# Install with: pip install -r requirements_hitl.txt

# Web server
flask>=3.0.0

# MCP tools integration (should already be installed if using agent_v2.py)
crewai>=0.86.0
crewai-tools>=0.17.0
python-dotenv>=1.0.0

# Note: These dependencies overlap with agent_v2.py requirements
# If you're already using the LLM agent, you likely have everything needed

